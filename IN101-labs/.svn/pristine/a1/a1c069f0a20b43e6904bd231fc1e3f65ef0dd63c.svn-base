#include <stdio.h>
#include "load-maze.h"

int main(void) {
    LoadMap("maze-ex.txt");

    int dimension = GetDim();

    printf("dimension: %d\n\n", dimension);

    map[2][3] = 'B';

    PrintMatrix();

    Clean();

    return 0;
}
